import Vue from 'vue'

import Datepicker from './components/Datepicker'


/* eslint-disable no-new */
new Vue({
  el: '#app',
  components : {
  	datepicker 	: Datepicker,
  },
  //render: h => h(App)
})
